const Manager = require("../lib/manager"); 
const Employee = require("../lib/employee"); 

test("Can set office number by means of constructor arguments", () => { 
    const testValue = 100; 
    const e = new Manager("Doi")
})